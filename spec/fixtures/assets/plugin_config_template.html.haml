
%label{ :name => 'my_plugin_config' } My Plugin Config
%input{ :type => 'text', :name => 'my_plugin_config' }

%select{ :name => 'content_type' }
  {{#each content_types}}
  %option{ :value => '{{ this.slug }}' } {{ this.name }}
  {{/each}}
